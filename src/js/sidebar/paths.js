
// decoshop.sidebar.paths

{
	init() {

	},
	dispatch(event) {
		let APP = decoshop,
			Self = APP.sidebar.paths,
			el;
		// console.log(event);
		switch (event.type) {
			// custom events
			case "show-color-values":
				break;
		}
	}
}
